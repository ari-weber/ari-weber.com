---
import GalleryPage from '../layouts/GalleryPage.astro';
import type { ImageMetadata } from 'astro';  // ← required
import { globToPhotos } from '../utils/photos';

<<<<<<< HEAD
const photos = globToPhotos(
  import.meta.glob<{ default: ImageMetadata }>(
    '../assets/images/events-concerts/*.{jpg,jpeg,JPG,png,PNG,webp}',
    { eager: true }
  )
);
photos.sort((a, b) => b.alt.localeCompare(a.alt));
=======
const photos = [
  { src: '/images/events/Greece_Party.jpg', alt: 'Party in Greece', title: 'Greece Party' },
];

const schema = {
  "@context": "https://schema.org",
  "@type": "ImageGallery",
  "name": "Event & Concert Photography",
  "description": "Event and concert photography by Ari Weber.",
  "url": "https://ari-weber.com/events/",
  "author": { "@type": "Person", "name": "Ari Weber" },
};
>>>>>>> 9e4cf3f97996e1a46703293e98ada112f0807fff
---

<GalleryPage
  title="Events & Concerts — Ari Weber Photography"
  description="Event and concert photography by Ari Weber."
  eyebrow="Collection 03"
  heading="Event & Concert"
  photos={photos}
/>
